{
  "name": "toolmatch",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for f in js/*.js; do node --check \"$f\" || exit 1; done",
    "validate": "node scripts/validate-data.js",
    "test": "npm run check && npm run validate && node --test"
  }
}
